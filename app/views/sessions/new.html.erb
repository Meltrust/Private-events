<div>
  <%= form_with(url: login_path, scope: :session, local: true) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "Name"%>
    <br>
    <div class="actions">
      <%= f.submit "Sign Up" %>
    </div>
  <%end%>
</div>
